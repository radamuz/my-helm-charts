apiVersion: v1
kind: Service
metadata:
  name: {{ include "filestash-onlyoffice.fullname" . }}
  labels:
    {{- include "filestash-onlyoffice.labels" . | nindent 4 }}
spec:
  type: {{ .Values.onlyoffice.service.type }}
  ports:
    - port: {{ .Values.onlyoffice.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "filestash-onlyoffice.selectorLabels" . | nindent 4 }}
